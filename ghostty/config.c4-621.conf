# Machine-specific Ghostty config for c4-621 (Thinkpad X220)
# Smaller font size for this display
# Note: Software rendering (LIBGL_ALWAYS_SOFTWARE, GALLIUM_DRIVER) is set via
# the ghostty-c4-621 wrapper script to avoid breaking Hyprland

font-size = 10
