#!/bin/bash
# Open new Firefox window from existing instance or launch if not running

# This will create a new window from the existing Firefox instance if running,
# or launch Firefox if it's not running
firefox --new-window &
