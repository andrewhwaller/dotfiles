# Machine-specific monitor configuration for c4-621 (Thinkpad X220)
# This overrides the default monitors.conf settings
# X220 has 1366x768 display

# Format: monitor = name, resolution, position, scale
# Note: Hyprland only supports scale >= 1.0 (no scaling below 100%)
# Scale options: 1 = 100%, 1.25 = 125%, 1.5 = 150%, 2 = 200%
# For "smaller" UI elements, use scale = 1 and adjust app-specific font sizes
monitor = , preferred, auto, 1
